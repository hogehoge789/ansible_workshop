---
- name: Get Inventory
  hosts: localhost
  connection: local
  gather_facts: False

  tasks:
    - name: Refresh inventory
      meta: refresh_inventory

- name: Software Instale
  hosts: tag_Role_cent_web
  remote_user: ec2-user
  become: true
  gather_facts: False
  roles:
    - yum_install
  
