---
- name: Get Inventory
  hosts: localhost
  connection: local
  gather_facts: False

  tasks:
    - name: Refresh inventory
      meta: refresh_inventory

- name: Software Instale
  hosts: tag_role_cent-web
  become: true
  gather_facts: False
  roles:
    - yum
  
